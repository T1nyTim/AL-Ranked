"""AniList Media Ranker.

This package provides tools to fetch and rank media (anime and manga) from the AniList GraphQL API
based on various criteria like type, status, country of origin, and genre. It supports generating
CSV files for each media category, offering an organized view of media rankings.

Modules:
    __main__: Orchestrates the process of fetching, ranking, and exporting media data.

The package is designed to interact with the AniList API, retrieve specific media information, and
process this data into a ranked format, which is then conveniently exported to CSV files for
analysis or record-keeping.

Usage:
    Execute the main module to start the data retrieval and export process.
"""
